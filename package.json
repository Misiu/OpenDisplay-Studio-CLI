{
  "name": "opendisplay-studio-cli",
  "version": "0.1.1",
  "description": "Local development CLI for OpenDisplay Studio widgets",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "odstudio": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "check": "tsc -p tsconfig.json --noEmit && vitest run"
  },
  "dependencies": {
    "@trmnl/picker": "0.2.0",
    "chokidar": "^4.0.3",
    "commander": "^14.0.0",
    "express": "^5.1.0",
    "fflate": "^0.8.2",
    "liquidjs": "^10.21.1",
    "open": "^10.2.0",
    "yaml": "^2.8.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.18.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
